name: Deploy — Production

# ─────────────────────────────────────────────────────────────────────────────
# Triggert bij een versie-tag op main (v*.*.*) OF handmatig via workflow_dispatch.
#
# VEILIGHEIDSMECHANISMEN:
#   1. Tests moeten slagen voordat deployment start
#   2. Verplichte handmatige goedkeuring via GitHub Environment 'production'
#   3. Deploy gebruikt de exacte versie-tag, nooit alleen :latest
#   4. Health check + automatische rollback bij mislukking
#
# Vereiste GitHub Secrets (naast staging-secrets):
#   PROD_SSH_KEY, PROD_SSH_HOST, PROD_SSH_USER
#   PROD_DB_PASSWORD, PROD_JWT_SECRET_KEY, PROD_ADMIN_PASSWORD
# ─────────────────────────────────────────────────────────────────────────────

on:
  push:
    tags: ['v*.*.*']
  workflow_dispatch:
    inputs:
      version_tag:
        description: "Versie om te deployen (bijv. v0.4.0)"
        required: true
        type: string

env:
  REGISTRY:       ghcr.io
  BACKEND_IMAGE:  ghcr.io/rhadix2026/uitvraag-backend
  FRONTEND_IMAGE: ghcr.io/rhadix2026/uitvraag-frontend

jobs:
  test:
    name: Pytest — backend
    runs-on: ubuntu-latest
    env:
      KIK_ENV: test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt
      - name: Installeer dependencies
        working-directory: backend
        run: pip install -r requirements-dev.txt
      - name: Voer tests uit
        working-directory: backend
        run: pytest tests/ --tb=short -v

  build:
    name: Build & push productie-images
    runs-on: ubuntu-latest
    needs: test
    permissions:
      contents: read
      packages: write
    outputs:
      version: ${{ steps.version.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
      - name: Bepaal versie-tag
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "tag=${{ inputs.version_tag }}" >> $GITHUB_OUTPUT
          else
            echo "tag=${GITHUB_REF_NAME}" >> $GITHUB_OUTPUT
          fi
      - uses: docker/setup-buildx-action@v3
      - name: Login naar GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build & push backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          no-cache: true
          tags: |
            ${{ env.BACKEND_IMAGE }}:${{ steps.version.outputs.tag }}
            ${{ env.BACKEND_IMAGE }}:latest
      - name: Build & push frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          no-cache: true
          build-args: |
            VITE_API_URL=
            VITE_KIK_ENV=production
            VITE_APP_VERSION=${{ github.ref_name }}
          tags: |
            ${{ env.FRONTEND_IMAGE }}:${{ steps.version.outputs.tag }}
            ${{ env.FRONTEND_IMAGE }}:latest

  deploy:
    name: Deploy naar productie
    runs-on: ubuntu-latest
    needs: build
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Log productie-deployment
        run: |
          echo "════════════════════════════════════════"
          echo "  PRODUCTIE DEPLOYMENT — Rhadix Uitvraag"
          echo "  Versie:  ${{ needs.build.outputs.version }}"
          echo "  Door:    ${{ github.actor }}"
          echo "  Tijd:    $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "════════════════════════════════════════"
      - name: Kopieer docker-compose.prod.yml naar server
        uses: appleboy/scp-action@v0.1.7
        with:
          host:     ${{ secrets.PROD_SSH_HOST }}
          username: ${{ secrets.PROD_SSH_USER }}
          key:      ${{ secrets.PROD_SSH_KEY }}
          source:   docker-compose.prod.yml
          target:   /opt/kik-app/
      - name: Deploy met automatische rollback bij mislukking
        uses: appleboy/ssh-action@v1.0.3
        with:
          host:     ${{ secrets.PROD_SSH_HOST }}
          username: ${{ secrets.PROD_SSH_USER }}
          key:      ${{ secrets.PROD_SSH_KEY }}
          script: |
            VERSION="${{ needs.build.outputs.version }}"
            COMPOSE_CMD="docker compose -p kik-prod -f /opt/kik-app/docker-compose.prod.yml --env-file /opt/kik-app/.env.production"

            PREV_VERSION=$(docker inspect kik-backend --format '{{.Config.Image}}' 2>/dev/null | awk -F: '{print $NF}' || echo "")
            echo "Vorige versie: ${PREV_VERSION:-onbekend} | Nieuwe versie: $VERSION"

            cat > /opt/kik-app/.env.production << 'ENVEOF'
            GHCR_ORG=rhadix2026
            DB_USER=kik
            DB_NAME=kik_starter
            FRONTEND_PORT=5176
            BACKEND_PORT=8012
            # ── Authenticatie & autorisatie (menselijk) ──────────────────────
            # Lokale wachtwoord-login uit; authenticatie verloopt via SSO.
            LOCAL_LOGIN_ENABLED=0
            # Applicatietoegang via de centrale apps-claim. Vastgestelde
            # PRODUCTIEBASELINE: on — weigeren met 403. Raakt NIET de externe API.
            # ROLLBACK zonder release: deze regel op 'warn' of 'off' zetten in
            # /opt/kik-app/.env.production en de backend herstarten; let op dat
            # een volgende deploy deze waarde opnieuw schrijft.
            APP_ACCESS_ENFORCE=on
            # ── Externe API (machine-to-machine) ─────────────────────────────
            # Oude password-grant uit; clienttokens 60 minuten geldig.
            KSAPI_ALLOW_PASSWORD_GRANT=0
            KSAPI_TOKEN_EXPIRE_MINUTES=60
            # DEPRECATED — niets leest KIK_ADMIN_* nog; opschoning volgt apart.
            KIK_ADMIN_EMAIL=admin@kik-starter.nl
            ENVEOF
            echo "KIK_VERSION=$VERSION"                                  >> /opt/kik-app/.env.production
            echo "DB_PASSWORD=${{ secrets.PROD_DB_PASSWORD }}"           >> /opt/kik-app/.env.production
            echo "JWT_SECRET_KEY=${{ secrets.PROD_JWT_SECRET_KEY }}"     >> /opt/kik-app/.env.production
            echo "KIK_ADMIN_PASSWORD=${{ secrets.PROD_ADMIN_PASSWORD }}" >> /opt/kik-app/.env.production
            # Externe API-clients: base64-JSON met bcrypt-hashes. Base64 voorkomt
            # dat de `$` uit bcrypt-hashes in de env-file wordt geïnterpreteerd.
            #
            # Het secret hoort ALTIJD te bestaan, ook als er geen afnemers zijn.
            # Gebruik dan de expliciete lege registratie W10= (base64 van "[]").
            # GitHub staat geen leeg secret toe, dus "bestaat maar leeg" wordt zo
            # uitgedrukt. De applicatie valt in alle gevallen fail-closed terug
            # (ontbrekend, leeg, W10= of onbruikbaar => tokenroute geeft 503), maar
            # met W10= is de bedoeling expliciet in plaats van impliciet.
            KSAPI_REG="${{ secrets.PROD_KSAPI_CLIENTS }}"
            echo "KSAPI_CLIENTS=$KSAPI_REG"                              >> /opt/kik-app/.env.production
            if [ -z "$KSAPI_REG" ]; then
              echo "LET OP: PROD_KSAPI_CLIENTS ontbreekt of is leeg — externe API fail-closed (503)"
            elif [ "$KSAPI_REG" = "W10=" ]; then
              echo "PROD_KSAPI_CLIENTS = lege registratie ([]) — externe API fail-closed (503), geen afnemers"
            else
              echo "PROD_KSAPI_CLIENTS gezet — externe API actief met geregistreerde client(s)"
            fi
            chmod 600 /opt/kik-app/.env.production

            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            if ! $COMPOSE_CMD pull; then
              echo "✗ Pull mislukt — containers onaangeroerd, deploy afgebroken"; exit 1
            fi
            $COMPOSE_CMD up -d --remove-orphans
            echo "Containers gestart, health check..."

            HEALTHY=false
            for i in $(seq 1 18); do
              STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8012/api/health 2>/dev/null || echo "000")
              if [ "$STATUS" = "200" ]; then HEALTHY=true; echo "✓ Productie bereikbaar na poging $i"; break; fi
              echo "  Poging $i/18 — HTTP $STATUS — wacht 5s..."; sleep 5
            done

            if [ "$HEALTHY" = "true" ]; then
              echo "✓ DEPLOY GESLAAGD: $VERSION"; $COMPOSE_CMD ps; exit 0
            fi

            echo "✗ HEALTH CHECK MISLUKT — AUTOMATISCHE ROLLBACK"
            docker logs kik-backend --tail 50 || true
            if [ -n "$PREV_VERSION" ] && [ "$PREV_VERSION" != "$VERSION" ] && [ "$PREV_VERSION" != "latest" ]; then
              echo "Terugzetten naar: $PREV_VERSION"
              sed -i "s/KIK_VERSION=.*/KIK_VERSION=$PREV_VERSION/" /opt/kik-app/.env.production
              $COMPOSE_CMD up -d --remove-orphans
              sleep 15
              RB=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8012/api/health 2>/dev/null || echo "000")
              [ "$RB" = "200" ] && echo "✓ Rollback naar $PREV_VERSION geslaagd" || echo "✗ Rollback ook mislukt (HTTP $RB) — handmatige interventie nodig"
            else
              echo "Geen bruikbare vorige versie voor rollback (was: ${PREV_VERSION:-leeg})"
            fi
            exit 1
